# Hand-written, not generated by gleam: a manifest mixing a hex package with a
# `source = "local"` path dependency. Backs
# fetchGleamDeps-skips-local-path-dependencies -- gleam resolves a path
# dependency from the filesystem and never looks for it under build/packages,
# so the tree must omit it rather than fail on it.

packages = [
  { name = "gleam_stdlib", version = "1.0.5", build_tools = ["gleam"], requirements = [], otp_app = "gleam_stdlib", source = "hex", outer_checksum = "CEE5B6C076A85B45F60C585F4316C63EC8B7127C119D5738C3958A9C4D50404E" },
  { name = "a_sibling_package", version = "1.0.0", build_tools = ["gleam"], requirements = [], source = "local", path = "../a_sibling_package" },
]

[requirements]
gleam_stdlib = { version = ">= 1.0.0 and < 2.0.0" }
