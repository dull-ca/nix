{
  "name": "dull-nix-bun-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/bundle.js && node src/build-css.js"
  },
  "dependencies": {
    "esbuild": "^0.25.0",
    "lightningcss": "^1.30.0"
  }
}
